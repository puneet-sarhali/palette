export interface Palette {
    id?: number,
    created_at?: Date,
    bg: string,
    bgFocus: string,
    primaryClear: string,
    primaryDull: string,
    primaryVisible: string,
    accentClear: string,
    accentDull: string
    clear: string,
    dull: string,
    duller: string,
    likes: number
}